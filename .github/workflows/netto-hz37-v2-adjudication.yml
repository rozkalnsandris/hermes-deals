name: Netto hz37 held-out v2 adjudication

on:
  pull_request_target:
    types:
      - labeled

permissions:
  actions: read
  contents: read
  issues: read
  pull-requests: read

concurrency:
  group: hermes-deals-netto-hz37-v2-adjudication
  cancel-in-progress: false

env:
  TRIGGER_LABEL: audit:netto-hz37-adjudication-v1
  EXPECTED_OWNER_LOGIN: rozkalnsandris
  EXPECTED_OWNER_ID: "277435981"
  UPSTREAM_RUN_ID: "34132489970"
  UPSTREAM_ARTIFACT_ID: "10022687292"
  UPSTREAM_ARTIFACT: netto-heldout-v2-fbe3cfa143788607446d0095ae1f887354d10eb3-run-34132489970
  UPSTREAM_ARTIFACT_DIGEST: sha256:6a6394e91df0aab2681f7c042397cc4c572f4254e3f7df008840165f97cae2dc
  UPSTREAM_ARTIFACT_BYTES: "41514323"
  EXPECTED_TRUTH_SHA256: ee859fc903012a8c617caa63446ad889a4b4f237c324b9e6a43f2a792b43d52c
  EXPECTED_RETENTION_BLOB: a26ff71d73fee0c4189151279215a65ed2d54fbf
  EXPECTED_TRUTH_BLOB: 049b6891d9f18c7c915f3b2c323aa312b3acd368
  EXPECTED_TRUTH_RECEIPT_BLOB: 40728fa509bda734490cc8763019b770ab5a0928

jobs:
  authorize:
    name: Authorize merged-PR hz37 adjudication
    if: >-
      github.event_name == 'pull_request_target' &&
      github.event.action == 'labeled' &&
      github.event.label.name == 'audit:netto-hz37-adjudication-v1'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      sha: ${{ steps.auth.outputs.sha }}
      pr_number: ${{ steps.auth.outputs.pr_number }}
      actor: ${{ steps.auth.outputs.actor }}
    steps:
      - name: Validate owner, merged PR and main ancestry
        id: auth
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          WORKFLOW_REF: ${{ github.ref }}
        shell: bash
        run: |
          set -Eeuo pipefail
          python3 - <<'PY'
          import json, os, pathlib, urllib.request
          repo = os.environ["REPOSITORY"]
          if repo != "rozkalnsandris/hermes-deals" or os.environ["WORKFLOW_REF"] != "refs/heads/main":
              raise SystemExit("unexpected repository/workflow ref")
          event = json.loads(pathlib.Path(os.environ["GITHUB_EVENT_PATH"]).read_text())
          sender = event.get("sender") or {}
          if sender.get("login") != os.environ["EXPECTED_OWNER_LOGIN"] or sender.get("id") != int(os.environ["EXPECTED_OWNER_ID"]):
              raise SystemExit("hz37 adjudication actor is not repository owner")
          if (event.get("label") or {}).get("name") != os.environ["TRIGGER_LABEL"]:
              raise SystemExit("unexpected hz37 adjudication trigger label")
          number = int((event.get("pull_request") or {}).get("number") or 0)
          headers = {
              "Accept": "application/vnd.github+json",
              "Authorization": f"Bearer {os.environ['GH_TOKEN']}",
              "User-Agent": "hermes-deals-netto-hz37-adjudication-authorizer",
              "X-GitHub-Api-Version": "2022-11-28",
          }
          def get(url):
              with urllib.request.urlopen(urllib.request.Request(url, headers=headers), timeout=20) as response:
                  return json.load(response)
          pr = get(f"https://api.github.com/repos/{repo}/pulls/{number}")
          if not pr.get("merged") or not pr.get("merged_at"):
              raise SystemExit("hz37 adjudication accepts only merged pull requests")
          if pr["base"]["ref"] != "main" or pr["base"]["repo"]["full_name"] != repo:
              raise SystemExit("trigger PR was not merged into repository main")
          sha = str(pr.get("merge_commit_sha") or "")
          if len(sha) != 40:
              raise SystemExit("merged pull request SHA is invalid")
          comparison = get(f"https://api.github.com/repos/{repo}/compare/{sha}...main")
          if comparison.get("status") not in {"ahead", "identical"}:
              raise SystemExit("hz37 adjudication implementation is not reachable from current main")
          with pathlib.Path(os.environ["GITHUB_OUTPUT"]).open("a", encoding="utf-8") as handle:
              handle.write(f"sha={sha}\npr_number={number}\nactor={sender['login']}\n")
          PY

  adjudicate:
    name: Adjudicate exact frozen hz37 v2 candidate
    needs: authorize
    if: needs.authorize.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    permissions:
      actions: read
      contents: read
    outputs:
      adjudication_sha256: ${{ steps.run.outputs.adjudication_sha256 }}
      prediction_group_count: ${{ steps.run.outputs.prediction_group_count }}
      candidate_auto_single_count: ${{ steps.run.outputs.candidate_auto_single_count }}
      resolved_group_count: ${{ steps.run.outputs.resolved_group_count }}
      single_source_count: ${{ steps.run.outputs.single_source_count }}
      mixed_source_count: ${{ steps.run.outputs.mixed_source_count }}
      excluded_control_count: ${{ steps.run.outputs.excluded_control_count }}
      unresolved_group_count: ${{ steps.run.outputs.unresolved_group_count }}
      not_evaluable_count: ${{ steps.run.outputs.not_evaluable_count }}
      acceptance_all_pass: ${{ steps.run.outputs.acceptance_all_pass }}
      artifact_name: ${{ steps.run.outputs.artifact_name }}
      artifact_id: ${{ steps.upload.outputs.artifact-id }}
      artifact_digest: ${{ steps.upload.outputs.artifact-digest }}
    steps:
      - name: Checkout exact reviewed hz37 adjudication merge
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09 # v5.1.0
        with:
          ref: ${{ needs.authorize.outputs.sha }}
          fetch-depth: 0
          persist-credentials: false

      - name: Verify immutable repo evidence blobs
        shell: bash
        run: |
          set -Eeuo pipefail
          [[ "$(git rev-parse HEAD:audit/netto/hz37/independent-retention-receipt.json)" == "$EXPECTED_RETENTION_BLOB" ]]
          [[ "$(git rev-parse HEAD:audit/netto/hz37/completed-source-truth.json)" == "$EXPECTED_TRUTH_BLOB" ]]
          [[ "$(git rev-parse HEAD:audit/netto/hz37/completed-source-truth-receipt.json)" == "$EXPECTED_TRUTH_RECEIPT_BLOB" ]]
          [[ "$(sha256sum audit/netto/hz37/completed-source-truth.json | cut -d' ' -f1)" == "$EXPECTED_TRUTH_SHA256" ]]

      - name: Download and verify exact retained pre-truth artifact
        id: upstream
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
        shell: bash
        run: |
          set -Eeuo pipefail
          zip_path="${RUNNER_TEMP}/netto-hz37-v2-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}.zip"
          echo "zip_path=$zip_path" >> "$GITHUB_OUTPUT"
          ZIP_PATH="$zip_path" python3 - <<'PY'
          import hashlib, json, os, pathlib, urllib.error, urllib.parse, urllib.request
          repo = os.environ["REPOSITORY"]
          run_id = os.environ["UPSTREAM_RUN_ID"]
          name = os.environ["UPSTREAM_ARTIFACT"]
          expected_id = int(os.environ["UPSTREAM_ARTIFACT_ID"])
          expected_digest = os.environ["UPSTREAM_ARTIFACT_DIGEST"]
          expected_bytes = int(os.environ["UPSTREAM_ARTIFACT_BYTES"])
          headers = {
              "Accept": "application/vnd.github+json",
              "Authorization": f"Bearer {os.environ['GH_TOKEN']}",
              "User-Agent": "hermes-deals-netto-hz37-adjudication-upstream",
              "X-GitHub-Api-Version": "2022-11-28",
          }
          encoded = urllib.parse.quote(name, safe="")
          request = urllib.request.Request(f"https://api.github.com/repos/{repo}/actions/runs/{run_id}/artifacts?name={encoded}", headers=headers)
          with urllib.request.urlopen(request, timeout=20) as response:
              artifacts = json.load(response).get("artifacts") or []
          if len(artifacts) != 1:
              raise SystemExit(f"expected exactly one hz37 artifact, found {len(artifacts)}")
          artifact = artifacts[0]
          if int(artifact.get("id") or 0) != expected_id or artifact.get("name") != name or artifact.get("expired") is not False:
              raise SystemExit("hz37 artifact identity/expiry mismatch")
          if artifact.get("digest") != expected_digest or int(artifact.get("size_in_bytes") or 0) != expected_bytes:
              raise SystemExit("hz37 artifact API digest/size mismatch")
          workflow = artifact.get("workflow_run") or {}
          if int(workflow.get("id") or 0) != int(run_id):
              raise SystemExit("hz37 artifact workflow-run mismatch")

          class NoRedirect(urllib.request.HTTPRedirectHandler):
              def redirect_request(self, req, fp, code, msg, hdrs, newurl):
                  return None
          archive_request = urllib.request.Request(f"https://api.github.com/repos/{repo}/actions/artifacts/{expected_id}/zip", headers=headers)
          opener = urllib.request.build_opener(NoRedirect)
          try:
              opener.open(archive_request, timeout=20)
              raise SystemExit("artifact archive endpoint unexpectedly did not redirect")
          except urllib.error.HTTPError as error:
              if error.code not in {301, 302, 303, 307, 308}:
                  raise
              location = error.headers.get("Location")
          if not location:
              raise SystemExit("artifact archive redirect is missing")
          path = pathlib.Path(os.environ["ZIP_PATH"])
          if path.exists() or path.is_symlink():
              raise SystemExit("hz37 ZIP destination already exists")
          digest = hashlib.sha256(); size = 0
          with urllib.request.urlopen(location, timeout=120) as response, path.open("xb") as output:
              while True:
                  chunk = response.read(1024 * 1024)
                  if not chunk:
                      break
                  output.write(chunk); digest.update(chunk); size += len(chunk)
          if f"sha256:{digest.hexdigest()}" != expected_digest or size != expected_bytes:
              raise SystemExit("downloaded hz37 ZIP digest/size mismatch")
          PY

      - name: Run deterministic hz37 v2 post-truth adjudication
        id: run
        env:
          ZIP_PATH: ${{ steps.upstream.outputs.zip_path }}
        shell: bash
        run: |
          set -Eeuo pipefail
          output="${RUNNER_TEMP}/netto-hz37-adjudication-${GITHUB_RUN_ID}-${GITHUB_RUN_ATTEMPT}"
          mkdir -m 700 "$output"
          result="$(python3 tools/netto_hz37_v2_prediction_group_adjudication.py \
            --artifact-zip "$ZIP_PATH" \
            --truth audit/netto/hz37/completed-source-truth.json \
            --retention-receipt audit/netto/hz37/independent-retention-receipt.json \
            --truth-receipt audit/netto/hz37/completed-source-truth-receipt.json \
            --output "$output/adjudication.json")"
          printf '%s\n' "$result"
          digest="$(sha256sum "$output/adjudication.json" | cut -d' ' -f1)"
          printf '%s  adjudication.json\n' "$digest" > "$output/SHA256SUMS"
          echo "output=$output" >> "$GITHUB_OUTPUT"
          echo "artifact_name=netto-hz37-v2-adjudication-run-${GITHUB_RUN_ID}" >> "$GITHUB_OUTPUT"
          RESULT_JSON="$result" OUTPUT_JSON="$output/adjudication.json" python3 - "$GITHUB_OUTPUT" <<'PY'
          import json, os, pathlib, sys
          summary = json.loads(os.environ["RESULT_JSON"])
          payload = json.loads(pathlib.Path(os.environ["OUTPUT_JSON"]).read_text(encoding="utf-8"))
          if payload.get("completed_source_truth_sha256") != os.environ["EXPECTED_TRUTH_SHA256"]:
              raise SystemExit("hz37 adjudication truth SHA mismatch")
          if payload.get("promotion_ready") is not False or payload.get("review_only") is not True:
              raise SystemExit("hz37 adjudication crossed promotion boundary")
          counts = payload["outcome_counts"]
          total = int(summary["prediction_group_count"]); resolved = int(summary["resolved_prediction_group_count"])
          with pathlib.Path(sys.argv[1]).open("a", encoding="utf-8") as handle:
              handle.write(f"adjudication_sha256={summary['adjudication_sha256']}\n")
              handle.write(f"prediction_group_count={total}\n")
              handle.write(f"candidate_auto_single_count={int(summary['candidate_auto_single_count'])}\n")
              handle.write(f"resolved_group_count={resolved}\n")
              handle.write(f"single_source_count={int(counts.get('single_source', 0))}\n")
              handle.write(f"mixed_source_count={int(counts.get('mixed_source', 0))}\n")
              handle.write(f"excluded_control_count={int(counts.get('excluded_control', 0))}\n")
              handle.write(f"unresolved_group_count={total - resolved}\n")
              handle.write(f"not_evaluable_count={int(summary['required_metric_not_evaluable_count'])}\n")
              handle.write(f"acceptance_all_pass={str(bool(summary['acceptance_all_pass'])).lower()}\n")
          PY

      - name: Upload hz37 adjudication artifact
        id: upload
        uses: actions/upload-artifact@b7c566a772e6b6bfb58ed0dc250532a479d7789f # v6.0.0
        with:
          name: ${{ steps.run.outputs.artifact_name }}
          path: ${{ steps.run.outputs.output }}
          if-no-files-found: error
          retention-days: 14

  report:
    name: Report held-out hz37 adjudication
    needs: [authorize, adjudicate]
    if: always() && needs.authorize.result == 'success'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      issues: write
      pull-requests: write
    steps:
      - name: Comment result and clear trigger label
        env:
          GH_TOKEN: ${{ github.token }}
          REPOSITORY: ${{ github.repository }}
          PR_NUMBER: ${{ needs.authorize.outputs.pr_number }}
          ACTOR: ${{ needs.authorize.outputs.actor }}
          IMPLEMENTATION_SHA: ${{ needs.authorize.outputs.sha }}
          ADJUDICATION_RESULT: ${{ needs.adjudicate.result }}
          ADJUDICATION_SHA256: ${{ needs.adjudicate.outputs.adjudication_sha256 }}
          GROUP_COUNT: ${{ needs.adjudicate.outputs.prediction_group_count }}
          AUTO_COUNT: ${{ needs.adjudicate.outputs.candidate_auto_single_count }}
          RESOLVED_COUNT: ${{ needs.adjudicate.outputs.resolved_group_count }}
          SINGLE_COUNT: ${{ needs.adjudicate.outputs.single_source_count }}
          MIXED_COUNT: ${{ needs.adjudicate.outputs.mixed_source_count }}
          EXCLUDED_COUNT: ${{ needs.adjudicate.outputs.excluded_control_count }}
          UNRESOLVED_COUNT: ${{ needs.adjudicate.outputs.unresolved_group_count }}
          NOT_EVALUABLE_COUNT: ${{ needs.adjudicate.outputs.not_evaluable_count }}
          ACCEPTANCE_ALL_PASS: ${{ needs.adjudicate.outputs.acceptance_all_pass }}
          ARTIFACT_NAME: ${{ needs.adjudicate.outputs.artifact_name }}
          ARTIFACT_ID: ${{ needs.adjudicate.outputs.artifact_id }}
          ARTIFACT_DIGEST: ${{ needs.adjudicate.outputs.artifact_digest }}
          RUN_ID: ${{ github.run_id }}
        shell: bash
        run: |
          set -Eeuo pipefail
          python3 - <<'PY'
          import json, os, urllib.error, urllib.parse, urllib.request
          repo, number = os.environ["REPOSITORY"], int(os.environ["PR_NUMBER"])
          body = (
              "## Netto held-out hz37 v2 adjudication result\n\n"
              f"- Trigger actor: `{os.environ['ACTOR']}`\n"
              f"- Adjudication job: **{os.environ['ADJUDICATION_RESULT']}**\n"
              f"- Implementation commit: `{os.environ['IMPLEMENTATION_SHA']}`\n"
              f"- Workflow run ID: `{os.environ['RUN_ID']}`\n"
              f"- Artifact: `{os.environ['ARTIFACT_NAME'] or 'not-created'}` (ID `{os.environ['ARTIFACT_ID'] or 'unavailable'}`)\n"
              f"- Artifact digest: `{os.environ['ARTIFACT_DIGEST'] or 'unavailable'}`\n"
              f"- Adjudication SHA256: `{os.environ['ADJUDICATION_SHA256'] or 'unavailable'}`\n"
              f"- Frozen groups: `{os.environ['GROUP_COUNT'] or 'unknown'}`; candidate auto-single `{os.environ['AUTO_COUNT'] or 'unknown'}`; resolved `{os.environ['RESOLVED_COUNT'] or 'unknown'}`\n"
              f"- Ownership outcomes: single `{os.environ['SINGLE_COUNT'] or 'unknown'}`, mixed `{os.environ['MIXED_COUNT'] or 'unknown'}`, excluded `{os.environ['EXCLUDED_COUNT'] or 'unknown'}`, unresolved `{os.environ['UNRESOLVED_COUNT'] or 'unknown'}`\n"
              f"- Required metrics NOT_EVALUABLE: `{os.environ['NOT_EVALUABLE_COUNT'] or 'unknown'}`\n"
              f"- All predeclared acceptance metrics PASS: **{os.environ['ACCEPTANCE_ALL_PASS'] or 'false'}**\n"
              "- Promotion ready: **false**\n"
              "- Parser behavior changed: **false**\n"
              "- Production deployment: **not authorized**\n"
              "- Database/Review/publication writes: **not authorized**\n"
          )
          headers = {
              "Accept": "application/vnd.github+json",
              "Authorization": f"Bearer {os.environ['GH_TOKEN']}",
              "Content-Type": "application/json",
              "User-Agent": "hermes-deals-netto-hz37-adjudication-reporter",
              "X-GitHub-Api-Version": "2022-11-28",
          }
          urllib.request.urlopen(urllib.request.Request(
              f"https://api.github.com/repos/{repo}/issues/{number}/comments",
              data=json.dumps({"body": body}).encode(), method="POST", headers=headers,
          ), timeout=20).close()
          remove = urllib.request.Request(
              f"https://api.github.com/repos/{repo}/issues/{number}/labels/{urllib.parse.quote(os.environ['TRIGGER_LABEL'], safe='')}",
              method="DELETE", headers=headers,
          )
          try:
              urllib.request.urlopen(remove, timeout=20).close()
          except urllib.error.HTTPError as error:
              if error.code != 404:
                  raise
          PY
