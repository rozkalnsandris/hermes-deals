{
  "name": "hermes-deals-ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "build": "vite build --config vite.config.js",
    "build:check": "vite build --config vite.config.js && node scripts/verify-build.mjs"
  },
  "devDependencies": {
    "vite": "8.1.5"
  }
}
